// script that lists all documents with name starting by Holberton in the collection school
const db = connect("mongodb://127.0.0.1:27017/" + db.getName());
const cursor = db.school.find({ name: { $regex: /^Holberton/ } });

cursor.forEach(doc => {
    printjson(doc);
});
